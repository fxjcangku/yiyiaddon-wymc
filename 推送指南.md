# 推送到 GitHub

仓库已在本地初始化并提交。

## 📦 本地仓库状态

✅ Git 初始化完成
✅ 所有文件已添加
✅ 首次提交完成

---

## 🚀 推送到 GitHub

### 方式 1：手动在 GitHub 创建仓库

1. 访问 https://github.com/new
2. 创建仓库 `yiyiaddon-wymc`（公开，不初始化）
3. 运行：

```powershell
cd D:\mcaddon\yiyiaddon-wymc
git remote add origin https://github.com/fxjcangku/yiyiaddon-wymc.git
git branch -M main
git push -u origin main
```

### 方式 2：使用 gh CLI（推荐）

```powershell
cd D:\mcaddon\yiyiaddon-wymc

# 创建仓库并推送（需要正确的 token 权限）
gh repo create fxjcangku/yiyiaddon-wymc --public --source=. --remote=origin --push

# 或者先手动创建仓库，再推送
gh repo view fxjcangku/yiyiaddon-wymc  # 检查是否存在
git remote add origin https://github.com/fxjcangku/yiyiaddon-wymc.git
git branch -M main
git push -u origin main
```

---

## 📋 推送后的工作

### 1. 配置仓库信息
- About → 添加描述和 Topics
- Topics: `minecraft`, `netease`, `java`, `mod-loader`, `instrumentation`

### 2. 创建 Release
```powershell
cd D:\mcaddon\yiyiaddon-wymc

# 创建标签
git tag -a v1.0.0-PHASE1 -m "First release"
git push origin v1.0.0-PHASE1

# 在 GitHub 创建 Release
gh release create v1.0.0-PHASE1 ^
  --title "YiYi WYMC Loader v1.0.0-PHASE1" ^
  --notes-file CHANGELOG.md ^
  --prerelease ^
  build/libs/YiYi-WYMC-Loader.jar ^
  build/libs/wymc-agent.jar
```

---

**等待你的确认后推送！**
